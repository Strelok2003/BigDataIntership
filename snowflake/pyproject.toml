[project]
name = "snowflake"
version = "0.1.0"
description = ""
authors = [
    {name = "Nika Modebadze",email = "nika.modebadze@innowise.com"}
]
requires-python = ">=3.13,<3.15 || >3.15"
dependencies = [
    "apache-airflow (>=3.2.1,<4.0.0)",
    "apache-airflow-providers-snowflake (>=6.12.2,<7.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
